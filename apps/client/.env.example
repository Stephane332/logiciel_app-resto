# URL de l'API.
#
# Web (PWA) : laissez vide, les appels passent par /api/v1 sur le même domaine.
# APK Android : obligatoire et absolue — l'application empaquetée n'a pas de « même domaine ».
VITE_API_URL=

# URL du serveur temps réel. Vide = même origine que la page.
VITE_REALTIME_URL=
